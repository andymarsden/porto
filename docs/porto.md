## porto is the code name for the chat functionality of the application.

The shell for the app is porto/+page.svelte

 currently the folders to use for this are:

- lib/actions
- lib/commands
- intent

we will be working towards a command pattern with manuakl registration.